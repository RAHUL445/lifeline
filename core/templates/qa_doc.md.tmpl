---
artifact: qa_doc
scope: "{{SCOPE}}"
phase: merge
created: "{{DATE}}"
cycle_id: "{{CYCLE_ID}}"
audience: qa
---

# QA handoff — {{SCOPE}}

> Code is a blackbox for this document. Everything below is observable behavior.

## 1. Feature flow

1. <user action> → <expected result>
2. <user action> → <expected result>

## 2. Preconditions & setup

- <environment / account / config / test data needed>

## 3. Test cases

### TC-1: <title>

- **Steps:**
  1. <step>
  2. <step>
- **Expected:** <observable result>

### TC-2: <title — edge case>

- **Steps:**
- **Expected:**

### TC-3: <title — negative case>

- **Steps:**
- **Expected:**

## 4. Smoke checklist (untested paths — must exercise manually)

- [ ] ⚠ <behavior backed by gapped code> — automation did not execute this path.
      Exercise: <concrete user action> → expect <result>. (`<file>`)

## 5. Known limitations / out of scope

- <what NOT to file bugs about>

<!-- end-of-file authoring hint
Generated by the handoff-docs skill at merge. Invariants (core/contracts/QA_DOC.md):
no implementation detail outside the §4 traceability anchors; ≥1 test case per functional
requirement; every coverage GAP produces exactly one §4 entry.
-->
