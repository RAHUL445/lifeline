---
artifact: reviewer_doc
scope: "{{SCOPE}}"
phase: merge
created: "{{DATE}}"
cycle_id: "{{CYCLE_ID}}"
audience: reviewer
---

# Reviewer handoff — {{SCOPE}}

## 1. Summary

<what changed + why, one paragraph>

## 2. Changes by wave/file

### Wave <id> — <subject>

| File | What changed | Why | Satisfies |
|---|---|---|---|
| `<path>` | <change> | <reason> | <FR-n / RCA item> |

## 3. Design decisions

- **<decision>** — chose <X> over <alternatives> because <reason>.

## 4. Risk areas

- `<path or area>` — <why it's the most likely place to be wrong; what to scrutinize>

## 5. Review findings & resolutions

| Lens | Severity | Location | Finding | Resolution |
|---|---|---|---|---|
| logic | <sev> | `file:line` | <issue> | <fixed in… / accepted because…> |

**Override audit:** <none | verbatim flow.md override events>

## 6. Test & coverage summary

Tests: <X passed, Y failed, Z skipped — per wave>

| Changed file | Coverage | Uncovered lines | Verdict |
|---|---|---|---|
| `<path>` | <pct>% | <ranges> | OK / GAP |

<GAP lines verbatim, if any:>
⚠ <file> — <pct>% coverage, lines <ranges> untested. Manually exercise this path.

<!-- end-of-file authoring hint
Generated by the handoff-docs skill at merge. Invariants (core/contracts/REVIEWER_DOC.md):
every functional requirement traceable in §2; every review finding in §5; every coverage
GAP line verbatim in §6; overrides never hidden.
-->
