# lifeline artifact roots (default + common relocations)
.lifeline/

# macOS
.DS_Store

# editor scratch
*.swp
*~

# founding spec — kept local, not published
PRD.md

# local working notes
todo.txt

# demo source (raw casts + recording guide) — only the rendered docs/demo-4x.gif is published
demo/
